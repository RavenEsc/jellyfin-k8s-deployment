apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jellyfin-charts.fullname" . }}-test-connection"
  labels:
    {{- include "jellyfin-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jellyfin-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
